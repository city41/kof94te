;; initChosenAvatarSprites
;;
;; Places the 12 sprites used to show the chosen team members onto the screen
;; at their correct places. Makes them two tiles tall but fills them with the empty tile
;; that way the rest of the hack never has to worry about positioning them, just replace the tiles



rts